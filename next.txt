Three real candidates left, in roughly impact-per-effort order:

  1. TODO #53 (mtc-keymaster/README-bugsandtodo.md) — align the cosigner-pin probe to hash SPKI DER instead of raw
  PEM-file bytes, matching the canonical form the mqc-3 server-side path now uses. Medium effort: edits in
  socket-level-wrapper-MQC/mqc_dnssec_pin.c + mtc-keymaster/tools/python/ca_dns_txt.py, plus a one-line republish of the
  live _mqc-ca.factsorlie.com TXT (the new SPKI-DER hash differs from the current raw-PEM hash). No new spec section; just
   makes the pin encoding-stable so re-emitting the same key with different PEM wrap-width / line-endings doesn't
  invalidate it.
  2. mqc-2 master plan Phase 5 — the cosigner-key TOFU hole on first contact (= socket-level-wrapper-MQC/reviews/README-mqc-2-issues.md issue #3 =
  mtc-keymaster/README-bugsandtodo.md TODO #9b). The most security-relevant remaining item but the biggest scope: I'd
  start by writing a per-branch plan from the existing ~/.claude/plans/lets-plan-to-implement-snoopy-rivest.md
  (leaf-bootstrap branch + CA-bootstrap branch), then implement one branch. Substantial — multi-commit.
  3. TODO #54 — raw-frame transcript hashing. Explicitly LOW; only worth doing alongside a protocol-version bump for some
  other reason.

  (mqc-2 P6 — revocation UX — is deferred indefinitely; not a real candidate.)

  My recommendation: TODO #53. It's the natural follow-up to mqc-3 (closes a fragility I documented while doing that
  work), self-contained, and gets the cosigner-pin path onto the same canonical-DER form as the CA-enrollment path before
  we accumulate more callers. Then Phase 5 next session as its own focused arc.

  Want #53, or skip ahead to Phase 5?

